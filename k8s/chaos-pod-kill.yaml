apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: nginx-pod-kill
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: web
  scheduler:
    cron: '@every 1m'