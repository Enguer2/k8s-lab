apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: app-servicemonitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: web
  endpoints:
  - port: web
    interval: 15s